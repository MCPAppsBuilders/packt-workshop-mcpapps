# Copy to .env before running `docker compose up`.

# Password for the VSCode web UI (http://localhost:8443)
WORKSHOP_PASSWORD=mcpapps

# Optional. Sign up at https://ngrok.com and paste your authtoken here.
# Leave empty to configure it later from the VSCode terminal.
NGROK_AUTHTOKEN=
