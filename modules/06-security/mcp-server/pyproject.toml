[project]
name = "mcp-fridge-server-secure"
version = "0.1.0"
description = "MCP Fridge Server with OAuth 2.1 (Keycloak)"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]==1.27.0",
    "pyjwt[crypto]>=2.10.1",
]
